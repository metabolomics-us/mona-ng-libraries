import { SpectackleDirective } from './spectackle.directive';

describe('SpectackleDirective', () => {
  it('should create an instance', () => {
    const directive = new SpectackleDirective();
    expect(directive).toBeTruthy();
  });
});
